import React from 'react';
import classes from '../css/Separator.module.css'

const Separator = () => {
    return (
        <div className={classes.separator}>

        </div>
    );
};

export default Separator;